# ── Core ──────────────────────────────────────────────────────
httpx>=0.27            # HTTP client for talking to Ollama (sync + async, streaming)
pydantic>=2.7          # typed models
pydantic-settings>=2.3 # .env-based config
python-dotenv>=1.0     # load .env

# ── RAG ───────────────────────────────────────────────────────
fastembed>=0.8         # all-MiniLM-L6-v2 embeddings on ONNX (no PyTorch)
faiss-cpu>=1.9         # vector store
pypdf>=4.2             # PDF text extraction
python-docx>=1.1       # .docx text extraction (local-file ingest)

# ── Voice ─────────────────────────────────────────────────────
faster-whisper>=1.2    # offline speech-to-text (Whisper on CTranslate2)
piper-tts>=1.6         # offline neural text-to-speech
sounddevice>=0.4       # mic capture + playback (PortAudio); the browser did this before
soundfile>=0.12        # decode Piper's WAV for playback

# ── CLI front end ─────────────────────────────────────────────
typer>=0.12            # command surface
rich>=13.7             # tables, colour, metrics strips
prompt_toolkit>=3.0    # REPL line editing + persistent history

# ── Disk analysis ─────────────────────────────────────────────
# Pillow arrives with fastembed; named here because the integrity checker
# depends on it directly to decode images rather than guess from byte markers.
pillow>=10.0

# ── Dev ───────────────────────────────────────────────────────
pytest>=8.0
